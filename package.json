{
  "name": "@zapx/nga-carousel",
  "version": "1.0.1",
  "description": "A schematics ng add carousel of Angular",
  "scripts": {
    "build": "gulp build",
    "test": "npm run build && jasmine ./src/**/*_spec.js && npm run clean",
    "link": "npm run build && yarn link && cd project && yarn link \"nga-carousel\"",
    "clean": "gulp clean:bundle",
    "format": "prettier \"**/*.ts\" --ignore-path ./.prettierignore --write"
  },
  "keywords": [
    "schematics",
    "angular",
    "nga-carousel"
  ],
  "author": "Playlistduong",
  "license": "MIT",
  "schematics": "./src/collection.json",
  "dependencies": {
    "@angular-devkit/core": "^9.0.1",
    "@angular-devkit/schematics": "^9.0.1",
    "typescript": "~3.7.4"
  },
  "devDependencies": {
    "@types/jasmine": "~3.5.0",
    "@types/node": "^12.11.1",
    "delete-empty": "^3.0.0",
    "gulp": "^4.0.2",
    "gulp-clean": "^0.4.0",
    "gulp-typescript": "^6.0.0-alpha.1",
    "jasmine": "^3.5.0",
    "prettier": "^1.19.1",
    "ts-node": "^8.6.2"
  }
}
